# Screenshots used in the README

These are referenced by `<img>` tags in `README.md`. Replace freely as the UI evolves; preferred: dark mode, 1440px wide browser, no dev tools open.

- `library.png` - Library page with at least two targets in the list, one target expanded showing multiple sessions.
- `project.png` - Project page open on a finished stack, history strip visible at the bottom, at least two revisions shown.
- `compare.png` - Compare view with two revisions side by side (or the before/after slider).
- `tonight.png` - Tonight planner with a few targets queued, rise/set bars visible.

The gallery view doesn't have enough published renders yet to be worth a screenshot; add `gallery.png` and an `<img>` block in the README once it does.
